import { CleanPathPipe } from './clean-path.pipe';

describe('CleanPathPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanPathPipe();
    expect(pipe).toBeTruthy();
  });
});
